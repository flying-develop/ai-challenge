# Ограничения по стеку

## Обязательные

- MCP-клиент использует официальный Python SDK (пакет `mcp`)
- MCP-подключения — единственное место, где допускается asyncio
- Конфигурация MCP-серверов хранится в config/mcp-servers.md
